# Languages, Compilers & Interpreters Project

Final project for the LCI Master’s programme, in which an interpreter was implemented for a small, typed functional language (`MiniTyFun`), and a compiler for an imperative language (`MiniImp`) that performs data flow analysis on the control flow graph of the intermediate language (`MiniRISC`).
